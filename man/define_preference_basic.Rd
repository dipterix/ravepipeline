% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preference-basic.R
\name{define_preference_basic}
\alias{define_preference_basic}
\alias{define_preference_multichoice}
\alias{define_preference_logical}
\title{Declare a preference with a common value constraint}
\usage{
define_preference_multichoice(
  pipeline,
  name,
  choices,
  default = choices[[1]],
  domain = PREFERENCE_DOMAINS,
  partial_match = FALSE,
  verbose = TRUE,
  force = FALSE
)

define_preference_logical(
  pipeline,
  name,
  default,
  domain = PREFERENCE_DOMAINS,
  verbose = TRUE,
  force = FALSE
)
}
\arguments{
\item{pipeline}{a \code{\link{PipelineTools}} instance}

\item{name}{preference name; may only contain letters, digits, underscores,
or dashes}

\item{choices}{character vector of the values the preference may take}

\item{default}{value used whenever the preference is unset; the first
element of \code{choices} for \code{define_preference_multichoice}, and a
length-one, non-missing logical for \code{define_preference_logical}}

\item{domain}{preference domain, one of \code{"default"},
\code{"graphics"}, \code{"analysis"}, or \code{"export"}}

\item{partial_match}{whether a unique abbreviation of a choice is accepted;
default is false. When true, the abbreviation is stored as it was given, and
reading it back with \code{pipeline$use_preference} expands it to the full
choice}

\item{verbose}{whether to emit trace-level logs; default is true}

\item{force}{whether to re-declare even when the stored declaration is
already current; default is false. See \code{define_preference} in
\code{\link{PipelineTools}}: declaring is skipped when the recorded version
is up to date, so these helpers can be called on every launch without
touching the disk. Set to \code{TRUE} only while developing}
}
\value{
Invisibly, a list with the stored value
(\code{preference_value}), whether the declaration was rewritten
(\code{metadata_updated}), and the preference \code{metadata}
}
\description{
Convenience wrappers around \code{define_preference} in
\code{\link{PipelineTools}} covering the two most common preference shapes:
a value restricted to a fixed set of choices
(\code{define_preference_multichoice}), and a \code{TRUE}/\code{FALSE} flag
(\code{define_preference_logical}). Each builds the \code{validator} for
you, so a declaration only needs the choices, or the default.

Re-declaring is cheap and safe: the stored preference metadata is rewritten
only when the declaration actually changes, and a value the user has already
chosen is never overwritten. See \code{\link{define_preference_colormap}}
for palette preferences.
}
\examples{

library(ravepipeline)
if(interactive() && length(pipeline_list()) > 0) {
  pipeline <- pipeline("power_explorer")

  res <- define_preference_multichoice(
    pipeline, name = "annotation_style",
    choices = c("none", "channel", "label"),
    domain = "graphics", partial_match = TRUE
  )
  res$preference_value                            # "none", the default

  pipeline$use_preference("annotation_style", value = "label")

  # a unique abbreviation is accepted and expands on read
  pipeline$use_preference("annotation_style", value = "chan")

  # anything else is rejected, and the stored value is left alone
  try({ pipeline$use_preference("annotation_style", value = "nope") })

  define_preference_logical(pipeline, name = "show_legend", default = TRUE)
  pipeline$use_preference("show_legend")
}

}
