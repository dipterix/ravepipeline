# This file is automatically generated by ravepipeline::mcptool_build(). 
# Please never edit this file by hand!
name: ravepipeline-mcp_tool_search_package_info
description: Retrieve detailed metadata and information about a specific R package,
  including version, dependencies, maintainer, and description. This provides comprehensive
  package details beyond basic help topics.
parameters:
  type: object
  properties:
    package_name:
      type: string
      description: The exact name of the package to get information about (e.g., "ravepipeline",
        "ggplot2", "dplyr"). This parameter is required.
      examples:
      - ravepipeline
      - ggplot2
      - dplyr
  required:
  - package_name
category: info
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    info:
      type: object
      description: 'List containing package metadata: version, title, description,
        author, maintainer, license, depends, imports, suggests, url, bugreports,
        and other DESCRIPTION fields'
    package:
      type: string
      description: Character, the package name
    success:
      type: boolean
      description: Logical, whether package information was retrieved successfully
implementation_example: |
  # Example output from calling the tool
  res <- ravepipeline:::mcp_tool_search_package_info("ravepipeline")
  strsplit(res$info$description, "\n")[[1]]
  #
  #> [1] "Defines the underlying pipeline structure for reproducible"               
  #> [2] "neuroscience, adopted by 'RAVE' (reproducible analysis and visualization" 
  #> [3] "of intracranial electroencephalography); provides high-level class"       
  #> [4] "definition to build, compile, set, execute, and share analysis pipelines."
  #> [5] "Both R and 'Python' are supported, with 'Markdown' and 'shiny' dashboard" 
  #>  [ reached 'max' / getOption("max.print") -- omitted 5 entries ]
execution_time: 0.002
