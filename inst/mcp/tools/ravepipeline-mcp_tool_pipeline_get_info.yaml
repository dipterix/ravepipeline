# This file is automatically generated by ravepipeline::mcptool_build(). 
# Please never edit this file by hand!
name: ravepipeline-mcp_tool_pipeline_get_info
description: 'Get detailed information about the currently loaded pipeline including
  targets, current settings, and available reports. Must call ravepipeline-mcp_tool_pipeline_load
  first. ## When to Use Need parameter schema, validation rules, pipeline details,
  or to discover available result target names. ## Preconditions - Pipeline must be
  loaded first ## Best Practices - DO: Check targets$Names to discover available result
  names (don''t call read without arguments) - DO: Use current_settings as reference
  for parameter names and structure - DO: Validate parameter names and types against
  schema before setting - DO: Use this to discover required parameters before configuration
  - DON''T: Guess target names or parameter names - DON''T: Read all results to discover
  names (expensive and slow)'
parameters:
  type: object
  properties: {}
category: info
returns:
  type: object
  properties:
    error:
      type: string
      description: Character, error message if success is FALSE
    available_reports:
      type: object
      description: List, available report templates with metadata
    current_settings:
      type: object
      description: List, current pipeline settings as key-value pairs
    targets:
      type: object
      description: Data frame of pipeline targets with Names and Description columns
    description:
      type: string
      description: Character, pipeline title/description
    path:
      type: string
      description: Character, file system path to pipeline directory
    name:
      type: string
      description: Character, pipeline name
    success:
      type: boolean
      description: Logical, whether information was retrieved successfully
implementation_example: |
  # Complete example:
  # The tool invokes an internal call to `ravepipeline:::mcp_tool_pipeline_get_info`
  # From within R. While users should not execute the function directly,
  # this function has the following equivalent implementation:
  # Example using pipeline_name called "power_explorer"
  # Prepare: load the pipeline
  pipe <- ravepipeline::pipeline("power_explorer")
  list(
    success = TRUE,
    name = pipe$pipeline_name,
    path = pipe$pipeline_path,
    description = pipe$description$Title,
    targets = head(pipe$target_table, 3),    # Truncated for display
    current_settings = head(pipe$get_settings(), 3), # Truncated
    available_reports = head(pipe$available_reports, 2) # Truncated
  )
  #
  #> $success
  #> [1] TRUE
  #> 
  #> $name
  #> [1] "power_explorer"
  #> 
  #> $path
  #> [1] "/Users/dipterix/rave_data/cache_dir/e7b555320895b06/pipelines/power_explorer"
  #> attr(,"target_name")
  #> [1] "power_explorer"
  #> attr(,"target_script")
  #> [1] "make-power_explorer.R"
  #> attr(,"target_directory")
  #> [1] "shared"
  #> 
  #> $description
  #> [1] "Explore Power or Amplitute of iEEG Spectrogram"
  #> 
  #> $targets
  #>           Names         Description
  #> 1 settings_path Check settings file
  #> 2      settings       Load settings
  #>  [ reached 'max' / getOption("max.print") -- omitted 1 rows ]
  #> 
  #>  [ reached 'max' / getOption("max.print") -- omitted 2 entries ]
execution_time: 0.077
